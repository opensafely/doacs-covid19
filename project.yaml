version: '3.0'

expectations:
  population_size: 10000

actions:

  generate_study_population:
    run: cohortextractor:latest generate_cohort --study-definition study_definition --index-date-range "2019-01-01 to 2021-10-01 by month" --output-dir=output --output-format=csv
    outputs:
      highly_sensitive:
        cohort: output/input_*.csv

  #generate_study_population_ethnicity:
    #run: cohortextractor:latest generate_cohort --study-definition study_definition_ethnicity --output-dir=output --output-format=csv
    #outputs:
      #highly_sensitive:
        #cohort: output/input_ethnicity.csv

  #join_ethnicity:
    #run: python:latest python analysis/join_ethnicity.py
    #needs: [generate_study_population, generate_study_population_ethnicity]
    #outputs:
      #highly_sensitive:
        #cohort: output/input*.csv

  generate_measures:
    run: cohortextractor:latest generate_measures --study-definition study_definition --output-dir=output
    needs: [generate_study_population]
    outputs:
        moderately_sensitive:
          measure_csv: output/measure_*.csv

  generate_notebook:
    run: jupyter:latest jupyter nbconvert /workspace/analysis/report.ipynb --execute --to html --template basic --output-dir=/workspace/output --ExecutePreprocessor.timeout=86400 --no-input
    needs: [generate_measures]
    outputs:
      moderately_sensitive:
        notebook: output/report.html

  #generate_cr_cl:
    #run: python:latest python analysis/calculate_cr_cl.py
    #needs: [join_ethnicity]
    #outputs:
      #highly_sensitive:
        #cohort: output/df_with_calculation_*.csv

  

  